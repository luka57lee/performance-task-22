import { describe, it } from 'mocha';
import { assert, AssertionError, expect } from 'chai';

const sinon = require('sinon');

describe('Subscription Controller Test', () => {
    it('Expected', () => {
        expect(true).to.be.true;
        expect(true).to.be.equal(true);
    });
});
