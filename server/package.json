{
  "name": "inquired-server",
  "version": "1.0.0",
  "description": "The backend of the performance task.",
  "main": "dist/index.js",
  "scripts": {
    "serve": "tsc-watch --onSuccess 'node dist/index.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rob",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.8.4"
  }
}
